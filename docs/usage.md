# Usage of djutils-build-parent

In the (parent) project's pom file, use:

```xml
<parent>
  <groupId>org.djutils</groupId>
  <artifactId>djutils-build-parent</artifactId>
  <version>...</version>
</parent>
```

All Maven plugin version numbers and default settings will be available.
